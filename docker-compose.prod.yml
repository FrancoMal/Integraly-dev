services:
  # --- Base de datos PRODUCCION ---
  # --- Permisos del directorio de backups PRODUCCION ---
  sqlserver-backup-init-prod:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: aicoding-sqlserver-backup-init-prod
    user: "0"
    volumes:
      - sqlserver_prod_backups:/backups
    entrypoint: ["bash", "-c", "chown -R 10001:0 /backups && chmod 775 /backups"]
    restart: "no"

  sqlserver-prod:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: aicoding-sqlserver-prod
    depends_on:
      sqlserver-backup-init-prod:
        condition: service_completed_successfully
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong@Passw0rd
      - MSSQL_PID=Express
    volumes:
      - sqlserver_prod_data:/var/opt/mssql
      - sqlserver_prod_backups:/backups
    networks:
      - aicoding-prod-net
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -C -Q "SELECT 1" || exit 1
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # --- Inicializacion de la DB PRODUCCION ---
  sqlserver-init-prod:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: aicoding-sqlserver-init-prod
    depends_on:
      sqlserver-prod:
        condition: service_healthy
    volumes:
      - ./db/init.sql:/init.sql
    networks:
      - aicoding-prod-net
    entrypoint: ["/opt/mssql-tools18/bin/sqlcmd", "-S", "sqlserver-prod", "-U", "sa", "-P", "YourStrong@Passw0rd", "-C", "-i", "/init.sql"]
    restart: "no"

  # --- Backend API PRODUCCION ---
  api-prod:
    build:
      context: ./src/Api
      dockerfile: Dockerfile
    container_name: aicoding-api-prod
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80
      - ConnectionStrings__DefaultConnection=Server=sqlserver-prod;Database=AIcoding;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;
      - MP_ACCESS_TOKEN=${MP_ACCESS_TOKEN:-}
      - MP_PUBLIC_KEY=${MP_PUBLIC_KEY:-}
      - PAYPAL_CLIENT_ID=${PAYPAL_CLIENT_ID:-}
      - PAYPAL_CLIENT_SECRET=${PAYPAL_CLIENT_SECRET:-}
      - PAYPAL_MODE=${PAYPAL_MODE:-live}
      - TRANSFER_CVU=${TRANSFER_CVU:-}
      - TRANSFER_ALIAS=${TRANSFER_ALIAS:-}
      - TRANSFER_TITULAR=${TRANSFER_TITULAR:-}
      - APP_BASE_URL=https://integraly.dev
      - PLAYWRIGHT_URL=http://playwright-prod:3001
    volumes:
      - api-uploads-prod:/app/uploads
      - sqlserver_prod_backups:/app/backups
      - /proc:/host_proc:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - aicoding-prod-net
    depends_on:
      sqlserver-prod:
        condition: service_healthy
    restart: unless-stopped

  # --- Playwright PRODUCCION ---
  playwright-prod:
    build:
      context: ./src/Playwright
      dockerfile: Dockerfile
    container_name: aicoding-playwright-prod
    environment:
      - NODE_ENV=production
    networks:
      - aicoding-prod-net
    restart: unless-stopped

  # --- Frontend PRODUCCION ---
  web-prod:
    build:
      context: ./src/Web
      dockerfile: Dockerfile
    container_name: aicoding-web-prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    networks:
      - aicoding-prod-net
    depends_on:
      - api-prod
    restart: unless-stopped

networks:
  aicoding-prod-net:
    driver: bridge

volumes:
  sqlserver_prod_data:
  sqlserver_prod_backups:
  api-uploads-prod:
